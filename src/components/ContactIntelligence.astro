<figure class="contact-specimen intelligence intelligence--terminal" data-intelligence data-intelligence-pose="terminal">
  <div
    class="intelligence__stage"
    data-intelligence-stage
    role="img"
    aria-label="An uploaded intelligence leaning over the contact terminal, resting both pixel hands on its edge and looking down at the words."
    aria-describedby="intelligence-invitation"
  >
    <span class="intelligence__poster" aria-hidden="true"></span>
    <canvas class="intelligence__canvas" data-intelligence-canvas width="384" height="256" aria-hidden="true"></canvas>
  </div>
  <figcaption id="intelligence-invitation" class="sr-only">A curious resident process follows the typing and your cursor.</figcaption>
</figure>

<script>
  import '../scripts/contact-intelligence';
</script>

<style>
  .contact-specimen.intelligence.intelligence--terminal {
    position: absolute;
    z-index: 3;
    inset: 0 0 auto;
    display: block;
    width: 100%;
    height: auto;
    min-height: 0;
    aspect-ratio: 3 / 2;
    margin: 0;
    overflow: visible;
    border: 0;
    background: transparent;
    pointer-events: none;
  }
  .intelligence__stage { position: absolute; inset: 0; }
  .intelligence__poster, .intelligence__canvas {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: contain;
    image-rendering: pixelated;
    color: var(--frame-ink);
  }
  .intelligence__poster {
    background: var(--frame-ink);
    mask: url('/assets/dither/intelligence-terminal.png') center / contain no-repeat;
    -webkit-mask: url('/assets/dither/intelligence-terminal.png') center / contain no-repeat;
  }
  .intelligence__canvas { opacity: 0; }
  .intelligence[data-renderer='pixels'] .intelligence__canvas { opacity: 1; }
  .intelligence[data-renderer='pixels'] .intelligence__poster { visibility: hidden; }
  .intelligence .sr-only { position: absolute; padding: 0; width: 1px; height: 1px; clip-path: inset(50%); }
</style>
